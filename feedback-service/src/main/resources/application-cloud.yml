eshop:
  services:
    catalogue:
      uri: lb://catalogue-service
      registration-id: keycloak
server:
  port: 8084
spring:
  application:
    name: feedback-service
  data:
    mongodb:
      uri: mongodb://localhost:27028/feedback-db
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://172.17.0.1:8082/realms/eshop
      client:
        registration:
          keycloak:
            provider: keycloak
            client-id: feedback-service
            client-secret: bHcUcuVY5PY5WUbtzy6JqwhfpqlJKjzr
            authorization-grant-type: client_credentials
            scope:
              - openid
          discovery:
            provider: keycloak
            client-id: feedback-service
            client-secret: bHcUcuVY5PY5WUbtzy6JqwhfpqlJKjzr
            authorization-grant-type: client_credentials
            scope:
              - discovery
        provider:
          keycloak:
            issuer-uri: http://172.17.0.1:8082/realms/eshop
springdoc:
  swagger-ui:
    oauth:
      client-id: feedback-service-swagger-ui
      use-pkce-with-authorization-code-grant: true
keycloak:
  uri: http://localhost:8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true